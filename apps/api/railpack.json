{
  "$schema": "https://schema.railpack.com",
  "build": {
    "command": "cd ../.. && npm ci && npx nx build api --configuration=production && cd apps/api && ls -la"
  },
  "deploy": {
    "startCommand": "node dist/main.js"
  }
}
