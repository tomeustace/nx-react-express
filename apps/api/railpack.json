{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": ["npm run railway:build"]
    }
  },
  "deploy": {
    "inputs": [
      {
        "step": "build",
        "include": ["dist"]
      }
    ],
    "startCommand": "npm run railway:start"
  }
}
