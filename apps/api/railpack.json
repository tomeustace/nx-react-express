{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "cd /app && npm ci && ./node_modules/.bin/nx build api --configuration=production && mkdir -p apps/api/dist-deploy && cp -r apps/api/dist/* apps/api/dist-deploy/ && cd apps/api && rm -rf dist && mv dist-deploy dist && ls -la dist/"
      ]
    }
  },
  "deploy": {
    "inputs": [
      {
        "step": "build",
        "include": ["dist"]
      }
    ],
    "startCommand": "node dist/main.js"
  }
}
